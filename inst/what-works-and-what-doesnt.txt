Was klappt und was nicht:

- foreach-old: Klappt mit allem

- doFuture sequential: klappt mit allem

- doFuture multisession: klappt mit allem aber kein console output wie bei foreach-old

- doFuture multiprocess/multicore: Error: length(results) == nbr_of_elements is not TRUE

- future_lapply: 
Using parallel framework 'future' with 'future_lapply'  and 'multisession' option.
Error : Number of classes is not equal to 2.
ROCR currently supports only evaluation of binary classification tasks.

- pbmclapply: LDA Beispiel Klappt nur auf Linux bzw auf Mac mit R --vanilla. Auf macOS infinite run 

- mclapply: LDA Beispiel Klappt nur auf Linux bzw auf Mac mit R --vanilla 
